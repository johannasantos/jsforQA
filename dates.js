// dates

var newDate = new Date()

// getting parts of a date

var newDate = new Date()

newDate.getFullYear()

newDate.getMonth() // los meses empiezan en 0

newDate.getDate()

var today = (newDate.getMonth() + 1) + "-" + newDate.getDate() + "-" + newDate.getFullYear()

var newDate = new Date()

newDate.getHours() // 24 HOURS CLOCK

newDate.getMinutes()

newDate.getSeconds()

newDate.getMilliseconds()

var time = newDate.getHours() + "-" + newDate.getMinutes() + "-" + newDate.getSeconds()

// setting new dates

var newDate = new Date()

newDate.setFullYear(2020)

newDate.setFullYear(2020, 4, 6)

newDate.setMonth(0, 1)

newDate.setDate(15)

newDate.setHours(10)

newDate.setHours(10, 25, 50)

newDate.setMinutes(30)

newDate.setMinutes(30, 30, 30)

newDate.getTime()












